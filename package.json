{
  "name": "microservices-av1",
  "version": "1.0.0",
  "description": "AV Prática: Microserviços com Node.js, TypeScript, Fastify e Docker",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install-all": "npm install",
    "build": "npm run build --workspaces --if-present",
    "start": "npm start --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
